import React, {useEffect, useState} from "react";

export const LoadingScreen = () => {
    return (
        <>
            <div className={"loadingScreen pageToTransit"}>Follow the Alpha</div>
        </>
    )
};